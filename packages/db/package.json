{
  "name": "@prism-era/db",
  "version": "0.1.0",
  "private": true,
  "description": "Database schema and migrations for Prism Era",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "exports": {
    ".": "./index.ts",
    "./schema": "./schema/index.ts"
  }
}
